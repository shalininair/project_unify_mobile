This directory contains a copy of the TypeScript source files for debug builds; it can be safely deleted and will be regenerated via the gulp ts task.

To omit this directory create a release build by specifying the scheme:
gulp ts --scheme release